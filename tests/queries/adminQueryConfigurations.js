/**
 * 管理員查詢組合配置
 *
 * 這是管理員查詢組合的唯一定義來源（Single Source of Truth）
 * 被以下檔案共用：
 * - tests/queries/adminQueries.test.js（測試 API）
 *
 * 注意：管理員集合的查詢與會員集合類似，主要是：
 * 1. 分頁查詢（無特殊篩選條件）
 * 2. isActive 狀態篩選
 * 3. includeDeleted 參數（是否包含已刪除管理員）
 */

// ===========================================
// 查詢組合列表
// ===========================================

const validQueryCombinations = [
  // ========================================
  // 1. 基本查詢（2 種）
  // 單一欄位排序 → Firestore 自動索引
  // ========================================
  {
    name: "基本查詢（預設排序）",
    params: {},
    description: "查詢所有管理員，按 createdAt 降序",
  },

  // ========================================
  // 2. 狀態篩選（2 種）
  // 等值查詢 + 排序 → 需要複合索引
  // ========================================
  {
    name: "僅查詢啟用管理員",
    params: { isActive: true },
    description: "查詢所有啟用狀態的管理員",
  },
  {
    name: "僅查詢停用管理員",
    params: { isActive: false },
    description: "查詢所有停用狀態的管理員",
  },

  // ========================================
  // 3. 包含已刪除（1 種）
  // 等值查詢 → 需要複合索引
  // ========================================
  {
    name: "包含已刪除管理員",
    params: { includeDeleted: true },
    description: "查詢所有管理員（包含已刪除）",
  },
];

// ===========================================
// 匯出配置
// ===========================================

module.exports = {
  validQueryCombinations,
};
