# Server Configuration
PORT=8080
NODE_ENV=development
IMAGE_VERSION=0.0.2

# 日誌等級：trace, debug(開發建議), info(生產建議), warn, error, fatal
LOG_LEVEL=debug

# 是否記錄 Response Body（開發環境建議：true，生產環境建議：false）
LOG_RESPONSE_BODY=true

# Response Body 最大記錄長度（bytes，預設 10000 = 10KB）
MAX_RESPONSE_BODY_LENGTH=10000

# Firebase Configuration
# 方法 1: 使用 Service Account JSON 檔案路徑 (不使用)
# GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# 方法 2: 使用 Base64 編碼的 Service Account (適用於 Cloud Run)
GOOGLE_CREDENTIALS_BASE64=

# Firebase Project ID
FIREBASE_PROJECT_ID=

# Firebase Web API Key (用於會員登入驗證)
# 取得方式: Firebase Console > Project Settings > General > Web API Key
# 位置: API和服務 > 憑證
FIREBASE_WEB_API_KEY=

# Firestore Database ID (如果使用命名資料庫)
# 預設為 (default)，如果你建立了新的命名資料庫，請在此設定資料庫 ID
FIRESTORE_DATABASE_ID=

# Firestore Warmup 配置
# 使用 listCollections() 建立 gRPC 連線池（僅 1 次讀取操作）
# 所有 collections 會自動複用此連線
# 生產環境建議：true，本地開發可設為 false 以加快啟動速度
ENABLE_FIRESTORE_WARMUP=true

# API Configuration
API_PREFIX=/api
CORS_ORIGIN=*

# Pagination
DEFAULT_PAGE_LIMIT=20
MAX_PAGE_LIMIT=100

# Seed Data Configuration
SEED_MEMBERS_COUNT=10
SEED_ORDERS_COUNT=50
SEED_PRODUCTS_COUNT=10
